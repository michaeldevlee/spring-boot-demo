# This changes the application server's port from 8080 to 8090
server.port=8090

# Message property
message=hello dev

# Mysql
spring.datasource.url=jdbc:sqlserver://training-db-2.database.windows.net:1433;database=training-db;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=michael@training-db-2
spring.datasource.password=SecretPassword!
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
spring.datasource.platform=mysql

# These tells Hibernate to initialize the database using classes with the
# @Entity annotations
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=none
